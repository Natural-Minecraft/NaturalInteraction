package id.naturalsmp.naturalinteraction.model;

public enum PostCompletionMode {
    SAME_NODES, // Repeat the same flow (default)
    ALTERNATE_NODES // Use a different root node for returning players
}
